# نشر موقع ريجينا على Cloudflare Pages (بديل نتليفاي)

نتليفاي متبلوك من كل شركات الإنترنت المصرية من يونيو ٢٠٢٦ (WE, Vodafone,
Orange, Etisalat) - أي زبون مصري لازم يفتح VPN عشان يشوف موقعك. Cloudflare
Pages مفيهاش المشكلة دي، ومعاها Functions بتعمل نفس شغل نتليفاي functions،
فمش محتاج تغيّر كود الفرونت إند خالص.

## خطوات النشر (أول مرة)

1. اعمل حساب على https://dash.cloudflare.com (مجاني)
2. من اليسار: **Workers & Pages** → **Create** → **Pages** → **Upload assets**
3. اسحب فولدر الموقع كله (اللي جواه index.html, assets/, functions/...) وارفعه
4. سمّي المشروع (مثلاً regina-eg) واضغط Deploy
5. بعد النشر: **Settings** → **Environment variables** → ضيف
   `HF_TOKEN` بقيمة مفتاح Hugging Face بتاعك (نفس اللي كان في نتليفاي)
   ثم اعمل Redeploy عشان المتغير يتفعّل
6. هيديك رابط زي `regina-eg.pages.dev` - ده بديل رابط نتليفاي، وشغال
   عادي في مصر من غير VPN

## لو عندك دومين خاص (مش رابط pages.dev)

Settings → Custom domains → ضيف الدومين واتبع تعليمات الـ DNS. لو الدومين
كان متوجّه لنتليفاي قبل كده، لازم تغيّر الـ DNS records يوجهوا لـ Cloudflare
بدل نتليفاي.

## ملاحظة عن زرار "الموكاب الواقعي"

لقيت إن السبب الحقيقي إن الزرار مش شغال مش بس الاستضافة — الكود كان بينادي
رابط Hugging Face قديم (`hf-inference`) اتلغى دعمه لموديلات زي الصورة دي من
نص ٢٠٢٥، بس هوجينج فيس مبيرجعش خطأ واضح، بيفشل بصمت. استبدلته بمكتبة
Hugging Face الرسمية نفسها (اللي بتختار السيرفر الصحيح تلقائي) وعملتلها
"bundle" في ملف واحد جاهز (`functions/api/generate-mockup.js`) عشان يشتغل
مباشرة من غير ما تحتاج تعمل npm install أو أي خطوة بناء.

الفولدر ده فيه:
- `netlify/functions/generate-mockup.mts` — نسخة قديمة بتاعة نتليفاي، فيها نفس مشكلة الرابط القديم، سيبتها بس متستخدمهاش
- `functions/api/generate-mockup.js` — النسخة الصحيحة الشغالة، دي اللي هتشتغل فعليًا بعد النشر على Cloudflare Pages

---

## تحديث ٢٤ سبتمبر ٢٠٢٦ — استوديو ٣٦٠° الجديد

صفحة `customize.html` بقت عارض 3D حقيقي. مفيش أي خطوة بناء إضافية مطلوبة،
بس فيه نقطتين مهمين قبل ما ترفع:

### ١ · الموديلات لازم تترفع مع الموقع

فولدر `assets/models/` فيه ٤ موديلات (‎7 MB إجمالًا) و**لازم يترفع مع باقي
الملفات**، وإلا العارض هيفضل يحمّل ومش هيبان حاجة:

```
assets/models/polo-half-sleeve.glb   1.29 MB
assets/models/polo-long-sleeve.glb   1.26 MB
assets/models/rounded.glb            4.44 MB
assets/models/cap.glb                0.04 MB
```

الملفات دي محسّنة (شيلت منها التكستشرات المطبوعة والـ color/UV attributes
اللي العارض مش بيستخدمها) — الحجم نزل من ‎14.65 MB لـ ‎7.03 MB من غير أي
تغيير في الشكل.

### ٢ · زرار „حمّل الموكاب“ بقى بيطلع من WebGL

الصورة بقت بتتولّد من الـ 3D renderer مباشرة بمقاس **1400×1760** للملابس
و**1400×1400** للكاب — فمفيش أي dependency جديدة، وكل حاجة بتشتغل أوفلاين.

### ٣ · حجم ملف الجافاسكريبت كبر شويّة (مقصود)

`assets/js/regina-3d.js` بقى **1.59 MB** بدل 1.43 MB، لأني أضفت مكتبة
`three-mesh-bvh` لتحسين الأداء. الزيادة ≈ 160 KB ومدمجة جوه الملف — يعني
**مفيش أي CDN أو dependency خارجية**، ولسه بيشتغل أوفلاين.

السبب: قبل كده أي حركة للشعار كانت بتعمل ٢٤٢ مليون اختبار وتجمّد الصفحة
**٢٦ ثانية**. دلوقتي **٠.٢ ثانية**. الـ 160 KB دي أرخص بكتير من التجميد.

### ملاحظة عن Cloudflare Pages

Cloudflare Pages عندها حد **٢٥ ميجا للملف الواحد** — أكبر موديل عندنا
‎4.44 MB، فالحمد لله مفيش مشكلة. (لو رفعت موديل أكبر من ٢٥ ميجا لازم
تحسّنه أو تقصّه الأول.)

### حاجات مطلوبة منك

- **`HF_TOKEN`** لازم يكون في Environment variables عشان زرار الموكاب
  الواقعي (الذكاء الاصطناعي) يشتغل. العارض ٣٦٠° نفسه **مش محتاج أي token**.
- **`og:image` و`og:url`** في الصفحة لسه `YOUR-DOMAIN-HERE.com` — استبدلهم
  بالدومين الحقيقي.
