# CHANGELOG — استوديو التصميم ٣٦٠° (customize.html)

آخر تحديث: ٢٤ سبتمبر ٢٠٢٦

---

## 1 · الموديلات ٣٦٠° اتضافت للصفحة

الصفحة كانت SVG مسطح (شكل تيشيرت مرسوم)، دلوقتي بقى فيه **عارض 3D حقيقي**
شغال بـ WebGL على الموديلات اللي رفعتها في فولدر `3d models`.

**الموديلات المستخدمة** (كلها اتنقلت لـ `assets/models/`):

| الموديل | بيظهر إمتى | الحجم بعد التحسين |
|---|---|---|
| `polo-half-sleeve.glb` | بولو كم نص | 1.29 MB |
| `polo-long-sleeve.glb` | بولو كم طويل | 1.26 MB |
| `rounded.glb` | تيشيرت رقبة دائرية | 4.44 MB |
| `cap.glb` | كاب | 40 KB |

**اللي بيعمله العارض:**
- لف ٣٦٠° كامل بالسحب (وبالإصبع على الموبايل)، وتقريب بالسكرول أو باللمس بإصبعين
- زرار **360°** بيلف الموديل لوحده
- أزرار **قدام / جنب / ضهر** بتودي الكاميرا للزاوية دي بالظبط
- **إضاءة الاستوديو / لون مسطح** — تبديل بين إضاءة واقعية بـ ٤ لمبات، أو إضاءة مسطحة عشان اللون يبان صافي
- `Reset View` + `Zoom +/−`
- بيفيت الكاميرا تلقائيًا على أي موديل، فلو غيّرت الموديلات تاني الحاجة بتظبط لوحدها

**بنية الجديد:**
- `src/regina-3d.src.js` — الكود المصدري للعارض (منظم ومعلّق)
- `assets/js/regina-3d.js` — نسخة مبنية جاهزة (`three.js` متجمّع جواها، مفيش CDN ولا npm)
- `assets/js/customizer.js` — اتحوّل كامل يتحكم في العارض بدل الـ SVG

> **مهم:** مفيش أي اعتماد على مكتبات خارجية وقت التشغيل. مكتبة three.js
> متجمّعة جوه ملف واحد، فالموقع شغال أوفلاين وعلى Cloudflare Pages عادي.

---

## 2 · الشعار مبقالوش يتشوّه  (كان فيه مشكلتين حقيقيتين)

اتأكدت من المشكلة بالقياس في متصفح حقيقي: رفعت شعار عرضه ٦٦٠ وبطوله ٢٠٠
(نسبة ٣.٣٠)، فطلع على الموكاب بمربع **١٤٠.٨ × ١٤٠.٨** — يعني نسبة ١.٠٠.
الشعار كان بيتكبس في مربع ويتشوه.

**السبب:** وسم `<image>` كان متثبّت على مربع `90×90` مع
`preserveAspectRatio="xMidYMid meet"`، فالسكيتشب كان بيصغّر الشعار جوه مربع
مش جوه الشكل بتاعه.

**الحل:** الشعار بقى **ديكال (decal) حقيقي على سطح الموديل**:
- شبكة مقسّمة بتترسم على القماش، وكل نقطة فيها بتترمي بـ raycast على سطح
  الموديل — فالشعار **بيتلوّى مع الكرمشة والانحناء** بدل ما يطفو فوقه
- أبعاده بتتحدد من نسبة الصورة الأصلية، فالشعار العريض بيفضل عريض
- النتيجة المقاسة بعد الإصلاح: نسبة **٣.٢٩** مقابل ٣.٣٠ الأصلية ✅
- **مستحيل يطلع على الضهر** لأن الديكال بيترسم على وش واحد بس (FrontSide)
  وليه اتجاه محدَّد
- تنظيم طبقات (polygonOffset + depthWrite:false) فمفيش تشويش مع القماش

---

## 3 · كل جزء في الملبوس بقى يتلوّن لوحده

الألوان كانت: لون واحد للقميص + لون واحد للياقة (والكاب لونين بس).
دلوقتي الألوان بقت **per-part حقيقية**:

### البولو (كم نص / كم طويل) — 5 أقسام
1. **الشعار**
2. **لون القماش**
3. **لون الأكمام** — مع خيار „نفس لون القماش“ → أي لون تختاره للجسم الكم
   بيتبعه تلقائيًا وانت مش محتاج تكرره
4. **لون الياقة والسوسته وأساور الكم** — كلهم مع بعض (زرار البولو كمان)
5. **لون الجيب** — مع خيار „نفس لون القماش“

### التيشيرت رقبة دائرية — 4 أقسام
لون القماش + لون الرقبة والأساور والحاشية (اللون التاني في الموديل).

### الكاب — 4 أقسام
الكاب كان **مصمّم كقطعة واحدة** في الموديل. فصلته في الكود لـ **١٥ قشرة
منفصلة** واتقسّموا أوتوماتيك حسب مكانهم ومساحتهم:
- **الكاب** (التاج)
- **الحافة والسير** — مع بعض
- **الزرار العلوي** — مع خيار „نفس لون الحافة“

### ملاحظات على الألوان
- كل وحدة ألوان فيها **٨ ألوان جاهزة + منتقي ألوان حر (color picker)**
- كل صف فيه اللون النشط معلَّم عليه إطار أحمر
- **مفيش أي branding غريب**: موديل الكاب الأصلي كان فيه **لوجو بوما متطبوع
  في التكستشر**. العارض بيمسح كل التكستشرات/الفيرتكس كولورز أول ما يحمّل أي
  موديل، فاللوجو ده **مش بيظهر ومش بيتصدّر** في الصور. (اتأكدت من ده بصريًا)

---

## 4 · التحميل (Download) كان **مكسور تمامًا** — اتصلح

دي كانت أخطر مشكلة ولقيتها في الاختبار:
زرار „حمّل الموكاب“ (وبالتالي زرار „ابعت التصميم على واتساب“) كان بيعمل
serialize للـ SVG ويرسمه على canvas. **الـ CSS variables (لون القميص) مبتعيش
بعد الـ serialize**، فالصورة اللي بتنزل كانت **قميص أسود سادة من غير شعار
خالص** — قِسناها: **صفر بيكسل شعار** على الصدر.

**بعد الإصلاح:** الصورة بقت بتطلع من الـ WebGL renderer نفسه، بمقاس ثابت
وواضح:
- **٧٠٠×٨٨٠** للملابس (والـ 2x يطلع ١٤٠٠×١٧٦٠)
- **٧٠٠×٧٠٠** للكاب
- بخلفية `#f2f2f2` فالصور تبان نضيفة على واتساب
- اللون والشعار والإضاءة كلهم موجودين (قِسنا ٢٥٥ ألف بيكسل بالألوان) ✅

---

## 5 · الباجات اللي كانت بتكسر الصفحة

### أ) أخطاء JavaScript عند التحميل
`customizer.js` القديم كان بيقرأ `trimSwatches` و`trimPicker` و`sleeveToggle`،
والصفحة كان فيها `trim-swatches` / `trim-color-picker` / `sleeve-toggle` —
الحاجات دي كانت `null`، وأول `addEventListener` عليها كان بيرمي Exception
بيوقف باقي الملف. **اتصلح** (الأكواد الحالية بتقرأ الـ ids الصح).

### ب) قسم البولو وقسم الرقبة الدائرية كانوا بيظهروا مع بعض
`grp-shirt` على المجموعتين كان بيكسب على قاعدة الإخفاء (specificity). دلوقتي
كل قسم بيظهر في وقته بس — والتحكم في الأكمام اتشال من صفحة الرقبة الدائرية
لأن الموديل ده مالوش قشرة كم منفصلة (كان هيبقى زرار ميّت).

### ج) الموبايل والـ RTL
اتأكدت إن الصفحة شغالة على مقاس ٤١٤px، وعلى العربية (`dir="rtl"`) ومع
الثيم الفاتح — مفيش أي خطأ ولا حاجة مقطوعة.

---

## 6 · الديب جوب (mockup بالذكاء الاصطناعي)

`ai-mockup.js` كان بيبعت `bodyPicker.value` رغم إن العنصر ده مش موجود،
فاللون الواقعي المبعوت للتوليد كان غلط. **اتصلح** — بقى بياخد المنتج والألوان
من حالة العارض الجديدة، والشعار بيتركّب على الصورة بنسبته الصحيحة.

> ملاحظة: توليد الموكاب الواقعي محتاج `HF_TOKEN` على الاستضافة (زي ما هو
> موضّح في `DEPLOY-CLOUDFLARE.md`). الشعار نفسه بيتحط على الصورة **في
> المتصفح** من غير أي API.

---

## 7 · تحسين حجم الموديلات: ‎14.65 MB → 7.03 MB (وفّرنا ‎52%)

الموديلات كانت جايبة معاها حاجات العارض مش بيستخدمها خالص:
- **تكستشرات مطبوعة** (خصوصًا الكاب: ‎4.76 MB من أصل ‎4.80 MB كانت صور!)
- **COLOR_0** (فيرتكس كولورز — التيشيرت المستدير كان بيتلوّن بيها)
- **TEXCOORD / TANGENT** (مش محتاجينها، إحنا مش بنِتكسشر)

| الموديل | قبل | بعد | الفرق |
|---|---|---|---|
| cap.glb | 4.80 MB | **0.04 MB** | ‎−99% |
| polo-half-sleeve.glb | 1.52 MB | **1.29 MB** | ‎−16% |
| polo-long-sleeve.glb | 1.50 MB | **1.26 MB** | ‎−16% |
| rounded.glb | 6.83 MB | **4.44 MB** | ‎−35% |
| **الإجمالي** | **14.65 MB** | **7.03 MB** | **‎−52%** |

الهندسة نفسها **مالمستهاش** (نفس عدد المثلثات ونفس الشكل)، والصفحة اترسمت
ورنت بنفس النتيجة بالظبط قبل وبعد (نفس أرقام البيكسلات في الاختبار).

---

## 8 · اللي اتاختبر بالفعل (مش مجرد كلام)

اتشغّل المتصفح الحقيقي (Chromium) على الصفحة واتعمل **١٥ اختبار** كلهم نجحوا
بـ **صفر error في الـ console**:

```
ok | page loads, viewer ready
ok | wide logo uploads
ok | logo keeps its 3.30 aspect ratio (not squashed)     ← 3.29 ✅
ok | fabric colour change works
ok | sleeve colour can differ from fabric
ok | collar/trim colour change
ok | back surface accepts its own logo
ok | product switch → long sleeve / round neck / cap
ok | cap crown + brim colours
ok | PNG export produces a non-blank image              ← 255,361 بيكسل ملوّن ✅
ok | drag rotates the model 360°                        ← 0° → −134° ✅
ok | flat-colour toggle
ok | reset
TOTAL ERRORS: 0
```

وكمان اتأكدت إن **الـ ٨ صفحات كلها** (index / about / caps / hats / tshirts /
coverall / graduation / customize) بتفتح من غير أخطاء ومن غير صور مكسورة.

---

## 9 · الملفات اللي اتغيرت / اتضافت

**اتعدّلت:**
- `customize.html` — الصفحة كلها بقت استوديو ٣٦٠°
- `assets/js/customizer.js` — اتحوّل كامل
- `assets/js/ai-mockup.js` — بيقرا الحالة الجديدة

**اتضافت:**
- `assets/js/regina-3d.js` — العارض (three.js متجمّع جواه)
- `assets/models/*.glb` — الموديلات الأربعة محسّنة
- `src/regina-3d.src.js` — الكود المصدري المعلّق

**مامسستهاش:** باقي الصفحات، الـ CSS العام، `functions/`، `netlify/`،
والـ build.

---

## 10 · حاجات لسه محتاجة منك

1. **`og:image` و`og:url`** في الصفحة لسه `YOUR-DOMAIN-HERE.com` — استبدلهم
   بالدومين الحقيقي (باقي الصفحات كمان فيها نفس الحاجة).
2. **`HF_TOKEN`** لازم يكون مضاف في Environment variables على الاستضافة عشان
   زرار الموكاب الواقعي يشتغل.
3. لو رفعت موديلات جديدة في `3d models`: الأفضل تحسّنها بنفس الطريقة قبل
   الرفع (شيل التكستشرات والـ UVs)، وحدّث المسارات في `src/regina-3d.src.js`
   في أول الملف (`MODELS`).

---

## 11 · إصلاحين اكتشفتهُم بعد التسليم (٢٥ سبتمبر ٢٠٢٦)

### أ) سحب الشعار بإيدك على الموكاب كان **مش شغال خالص**

الصفحة بتقول للمستخدم „اسحب الشعار على الموكاب تحرّكه بإيدك“، بس السحب كان
بيلف الموديل بدل ما يحرّك الشعار. السبب: العارض بيستخدم **raycaster واحد**
مشترك، وبنا الديكال بيحدّد له `far` بمقاس محدود (~1.4 × حجم الموديل). الكاميرا
بعدها بحوالي ٣٦٠٠ وحدة، فشعاع الاختيار كان **أقصر من إنه يوصل للشعار** —
فـ `overDecal()` كان دايمًا بيرجّع false وكل سحبة تتحوّل لدوران.

**الحل:** دالة `resetPickRange()` بتصفّر المدى قبل أي عملية اختيار.

**النتيجة المقاسة:** الشعار قبل السحب `s=0, t=0` → بعد السحب
`s=106.8, t=-22.8` ✅ واتأكدت إن الاستدارة نفسها لسه شغالة (`yaw 0° → −134°`).

### ب) مثلث طاير (stray triangle) لما الشعار يخرج عن حدود الملبوس

لو حطيت شعار كبير لدرجة إن رُكن من الديكال يطلع خارج الملبوس، الرُكن ده مكان
بيلاقي سطح تحته، فكان بيظهر **مثلث ملوّن طاير** جنب الياقة.

**الحل:** أي مثلث من الديكال فيه رأس مالقاش سطح تحته بيترصّ في نقطة واحدة
(degenerate) فمش بيرسم حاجة — يعني الديكال بيقص نفسه على حدود الملبوس.

**النتيجة المقاسة:** مع شعار مكبّر ٢.٦× — ٨١٩٢ مثلث، منهم **١١٥٠ اترصّوا**
و**٧٠٤٢** لسه بيرسموا. مفيش أي مثلث طاير في الصورة.

### اختبار تغطية الديكال (multi-mesh)
أكبر شعار دلوقتي بيتغطى على أكتر من قشرة في نفس الوقت، وده مقاس بالقمم:

| القشرة | عدد القمم تحت الشعار |
|---|---|
| `Front_FRONT_2234` (القماش الأمامي) | 2674 |
| `Pocket_FRONT_2244` (الجيب) | 1475 |
| `Button_FRONT_2249` (الزرار) | 61 |

يعني الشعار بيعبر من القماش للجيب للزرار بشكل متصل — نفس الحاجة اللي كانت
بتحاول تعملها النسخة التانية (multi-mesh projection).

**الاختبار الكامل بعد التعديل: ١٥/١٥ نجحوا، صفر error.**

---

## 12 · تحسينات الأداء ومكان الشعار (PERF-BVH-2026)

### أ) تجمّد الصفحة ٢٦ ثانية مع أي حركة للشعار

كل مرة الشعار بيتحرّك أو بيتكبّر أو بيتلف، الكود بيعيد بناء "الديكال": ٦٥×٦٥ =
**٤٢٢٥ نقطة**، وكل نقطة بتعمل اختبار شعاع ضد **٥٧٤٥٠ مثلث**. يعني **٢٤٢ مليون
اختبار** في المرة الواحدة.

| العملية | قبل | بعد |
|---|---|---|
| ضغطة واحدة على مكان الشعار | **٢٥٬٨٦٦ ms** | **١٩٠–٢٥٠ ms** |
| سحب الشعار بالماوس | ٢٦ ث/حركة | ناعم ٦٠ إطار/ث |
| اختبار الـ ١٥ خطوة | ٢١٢ ثانية | **٣٠ ثانية** |
| اختبار السحب والتغطية | ٢٩٩ ثانية | **١٢.٦ ثانية** |

**الحل:** **BVH** عن طريق `three-mesh-bvh` — البحث بقى O(log n) بدل
O(عدد المثلثات)، والـ BVH اتبنى مرة واحدة عند التحميل.

**النتائج مطابقة تمامًا:** النسبة ٣.٢٩ · التصدير ١٤٠٠×١٧٦٠ بـ ٢٥٥٬٣٦١ بكسل ·
الدوران ٠° → −١٣٤.٤° · التغطية (الأمامي ٢٦٧٤، الجيب ١٤٧٥، الزرار ٦١).
الإصلاح سرّع بس — ما غيّرش ولا نتيجة.

### ب) السحب ناعم، والإلزاق عند الإفلات
أثناء السحب الشعار بيتزحلق مع الماوس (خفيف)، ولما تسيب الماوس بيعمل إعادة
إلزاق كاملة على القماش مرة واحدة — نفس النتيجة، بس سلس.

### ج) أزرار مكان الشعار (جديد)
أربعة أزرار في لوحة الشعار: **وسط الصدر · صدر شمال · صدر يمين · الجيب**.

الأرقام بتتحسب من قياس حقيقي للوحة الأمامية، فبتظبط على كل موديل. زر
"الجيب" طلع `s = 106.4` والقياس المستقل لمركز الجيب `106.8` — يعني صح.

ولو الشعار عريض لدرجة إنه حيطلع بره الملبوس، الكود بيقيس عرضه الفعلي
وبيرجّعه لجوه، بهامش ٨٨٪ (مش ٩٤٪) لأن حدود اللوحة هي أوسع نقطة فيها.

**الاختبار النهائي: ١٥/١٥ · ٨/٨ صفحات · موبايل وعربي ٠ أخطاء.**

---

## 13 · توافق مع النسخة التانية + تنظيف حالة الأزرار (PARITY-2026)

### أ) اختبرت سيناريو النسخة التانية بالحرف على نسختي

النسخة التانية بتعمل اختبار بيدوس على: كاميرا (back/front)، زر مكان الشعار،
ولون من لوحة الجسم والحواف والكاب. جريت **نفس الترتيب بالظبط** على نسختي:

| الخطوة | النتيجة |
|---|---|
| كاميرا → جنب | ✅ الدوران ٠° → ٩٠° |
| كاميرا → قدام | ✅ الدوران ٩٠° → ٠° |
| زر مكان الشعار (صدر شمال) | ✅ الموضع ٠ → −٦٧.٥ |
| لون الجسم `#e0201f` | ✅ `#1e4fe0` → `#e0201f` |
| لون الحواف `#121212` | ✅ الياقة `#f5f5f0` → `#121212` |
| لوحة الكاب تظهر مع الكاب | ✅ |
| لون الكاب `#1e4fe0` | ✅ التاج صار `#1e4fe0` |

**٧/٧ نجحوا · صفر أخطاء في الكونسول.**

يعني نفس اللي بيعمله اختبارهم شغال عندي بالظبط. الفرق الوحيد في التسمية:
هما بيستخدموا `data-cam` وأنا `data-view` — والاتنين بيوصلوا لنفس النتيجة.

**ملاحظة مهمة:** اختبارهم بيبدأ بـ `if (!shirtSvg) return;` — يعني لو
جريتوه على نسختي، **بيخرج فورًا** لأني شلت الـ SVG المسطّح. اختبارهم مصمم
يتخطى أي صفحة مبنية على 3D.

### ب) إصلاح: زر مكان الشعار كان بيفضل مولّع بعد ما تغيّر المنتج

لما تدوس "صدر شمال" على البولو وتغيّر للكاب، الزر كان بيفضل **مضيّء** مع إن
مكان الشعار على الكاب مالوش معنى أصلاً.

**الحل:** دالة `clearPlacement()` واحدة بتنضّف الحالة، وبتتنادى من ٣ أماكن:
تغيير المنتج، تغيير الوش (أمامي/خلفي)، وزر "شيل الشعار".

**النتيجة المقاسة:**

| الحالة | قبل | بعد |
|---|---|---|
| بعد اختيار "صدر شمال" | `["chest-left"]` | `["chest-left"]` ✅ |
| بعد التحويل للكاب | `["chest-left"]` ❌ | `[]` ✅ |
| بعد الرجوع للبولو | `["chest-left"]` ❌ | `[]` ✅ |
| بعد "شيل الشعار" | `["pocket"]` ❌ | `[]` ✅ |

**الاختبار النهائي: ١٥/١٥ · ٧/٧ توافق · ٨/٨ صفحات · موبايل وعربي ٠ أخطاء.**

---

## 14 · أسماء أزرار المكان بقت حسب المنتج (LABELS-2026)

الزراير كانت مكتوب عليها "وسط الصدر · صدر شمال · صدر يمين" حتى على **الكاب** —
والكاب مالوش صدر أصلاً. ده كان لغوياً غلط ومربك.

**الحل:** كل منتج ليه أسماء خاصة بيه:

| الزر | بولو / تيشيرت | كاب |
|---|---|---|
| ١ | وسط الصدر · Chest centre | واجهة الكاب · Front panel |
| ٢ | صدر شمال · Chest left | يسار الكاب · Left panel |
| ٣ | صدر يمين · Chest right | يمين الكاب · Right panel |
| ٤ | الجيب · Pocket | (مخفي — الكاب مالوش جيب) |

الأسماء بتتبدّل أوتوماتيك مع تغيير المنتج، وبالعربي والإنجليزي. والأرقام
اللي بتحسب مكان الشعار نفسها ما اتغيرتش — بس التسمية.

**الاختبار: الأسماء اتأكدت بالعربي والإنجليزي على البولو والكاب، وزر الجيب
مخفي على الكاب.** وباقي كل الاختبارات زي ما هي: ١٥/١٥ · ٧/٧ توافق · ٨/٨ صفحات.
